# Handoff — 2026-07-04

## Session accomplishments (all verified working)

### 1. Deployment-ready installer with bundled PostgreSQL ✅ DONE
- `pgsql/` at project root: trimmed PostgreSQL 17.6 win-x64 binaries (~133 MB, gitignored). Recreate per MANUAL_Setup_Installation.md §1 (EDB zip, delete pgAdmin 4/StackBuilder/doc/include/lib/pgxs).
- `electron/postgres.js` (NEW): full DB lifecycle on app launch — generates `.env.local` with unique random credentials if missing, skips if DB port busy or non-localhost, else initdb → `%APPDATA%\whitevanops\pgdata`, start via pg_ctl (127.0.0.1 only), apply `resources/db/schema.sql`, bootstrap admin/admin. Sentinel `bootstrap-complete` gates first run; failure wipes pgdata and retries. Stops DB on app quit.
- `scripts/electron-build.js`: now concatenates prisma/migrations → `.next/db/schema.sql`.
- **Two hard-won gotchas (documented in CLAUDE.md + memory):**
  - `pg_ctl start/stop` MUST use `stdio: 'ignore'` in execFileSync (daemon inherits pipes → hang).
  - electron-builder silently drops `node_modules` from extraResources — package.json has an explicit second entry `{from: ".next/standalone/node_modules", to: "nextjs/node_modules", filter: ["**/*"]}`. Never remove. Pre-existing bug: all older installers had broken self-boot.
- Final installer: `dist-electron/WhiteVanOps Setup 0.1.0.exe` (244 MB, Jul 3 21:38). Fully smoke-tested: cold zero-config start, warm restart, port-in-use reuse, graceful DB stop, admin/admin login.

### 2. Graphics ✅ DONE (but see redesign below)
- `public/logo.svg` = brand master (van mark, dark badge, blue stripe). `node scripts/generate-icons.js` (devDeps: sharp, png-to-ico) regenerates logo.png, public/icons/icon-192/512, apple-touch-icon.png, src/app/favicon.ico.
- Logo used on login, change-password, dashboard sidebar, field picker; splash `electron/loading.html` redesigned with inline SVG copy.

### 3. PWA + QR ✅ DONE
- `src/app/manifest.ts` (start_url `/field`, standalone). Middleware PUBLIC_PATHS whitelists `/logo.png`, `/logo.svg`, `/icons`, `/apple-touch-icon.png`, `/manifest.webmanifest` (required — assets were being auth-redirected).
- Dashboard sidebar → **Field Access QR** button → `src/components/modals/FieldAccessModal.tsx` (deps: `qrcode` + `@types/qrcode`). Editable URL persisted in localStorage `wvo.fieldAccessUrl`; warns on localhost/plain-http.
- ModalType union in src/types.ts gained `"fieldAccess"`.

### 4. Marketing site ✅ DONE
- `marketing/index.html` — single-file static site, styled as a light spec-sheet. Contact section updated.
- All manuals updated (Setup/Admin/Field Tech). CLAUDE.md updated.

### 5. FRP P2P Network Access ✅ DONE
- Configured FRP (Fast Reverse Proxy) for direct P2P connections to bypass cloud hosting for field devices.

### 6. Ironclad Local Backup ✅ DONE
- Implemented `backup.js` automated pg_dump scheduler at 2:00 AM.
- Added Settings UI for "Target Directory Mirror".

### 7. Offline-First PWA ✅ DONE
- Setup `sw.js` and IndexedDB for `apiCache` and `syncQueue`.
- The field UI transparently switches to offline operations and flushes background queue when reconnected.

## 🌐 GitHub Pages deployment (2026-07-04, same session as redesign)

**Live site:** <https://rsm1274-art.github.io/WhiteVanOps/> — GitHub Pages, `main` branch root of
<https://github.com/rsm1274-art/WhiteVanOps> (GitHub user `rsm1274-art`; signs into GitHub with Google).

- **`marketing/` is now a git checkout of that repo** (the overall project is still NOT a git repo).
  Contents: `index.html`, `README.md`, `.nojekyll`. To publish site changes: commit in `marketing/` and
  `git push`. Auth works via Git Credential Manager (token already stored on this machine — no gh CLI
  installed; API calls were made with `git credential fill` piped into curl, token never echoed).
  Note: curl on this machine needs `--ssl-no-revoke` (schannel CRYPT_E_NO_REVOCATION_CHECK otherwise).
- **Pricing finalized and pushed:** Single Office **$500** one-time, Office + White Glove **$1000**
  one-time + setup, "Priority support · **$250 lifetime access**" (replaced `[SLA TBD]`). Placeholder
  pricing notes removed from index.html and README. Remaining placeholders: contact info + footer
  company name.
- Commits: `df8b836` initial site → `48227ac` README deploy note → `ecf898a` $250/$500 pricing
  (✅ deployed, live) → `9cd0b0a` lifetime-access line (**⚠ deploy was stuck when session ended**).

### ✅ RESOLVED: deploy of 9cd0b0a was stuck, fixed by Pages reset
GitHub's Pages pipeline wedged on the last commit: first "pages build and deployment" run failed with
`##[error]Deployment failed, try again later` (error_count 10 — GitHub-side), reruns then sat in
"queued" 45+ min (user cannot cancel these; they're GitHub-managed) even though githubstatus.com said
Pages was operational. **Fix that worked: delete and re-create the Pages config via API**
(`DELETE /repos/.../pages` → 204, then `POST /repos/.../pages` with source main// → 201) — this clears
the stuck queue and triggers a fresh build. Shortly after, the live site served
"Priority support · $250 lifetime access" (verified by curl). Remember this remedy if a Pages deploy
ever wedges in "queued" again.

## ✅ COMPLETED (later session, 2026-07-04): Professional redesign — all phases done

All four phases below were completed and browser-verified. Phase A tokens verified on login/dashboard;
Phase B: blue CTAs (tabs, modals, ConfirmDialog, SubmitButton), ruled modal headers (border-b-2 ink),
rounded-full bordered status pills, light login + change-password pages, blue active-nav rail in sidebar,
light/squared/blue loading.html. Phase C: marketing/index.html fully restyled light spec-sheet (SEC. 0x
mono labels with crosshair ticks, hairline panels, inline stroke SVG icons, light hero; dark reserved for
ownership band/phone demo/footer) — all JS demos intact and click-tested. Phase D: logo.svg squared
(rx 112→56) + warmed to stone palette, icons regenerated, installer rebuilt
(`dist-electron/WhiteVanOps Setup 0.1.0.exe`, Jul 4 06:25, 244 MB) with
`win-unpacked/resources/nextjs/node_modules/next` verified present. Lint + tsc clean.
Field page verified inheriting tokens. marketing/README.md branding note updated.
Manuals reviewed — no visual-style references existed, so no manual changes were needed.
Dev DB was re-bootstrapped this session and seeded (`admin/admin123` etc. per seed output); the seeded
cluster's PostgreSQL is left running on 5433.

## Original plan (for reference)

**Goal:** de-genericize app + marketing site. Direction: "engineered equipment, not SaaS startup" — light-first warm palette, squared corners, condensed signage typography. **User chose BLUE accent (engineering blue ~blue-700), not orange. Updates apply to BOTH app and website.**

### Approved plan (phases)
- **A. Design tokens — ✅ JUST COMPLETED (unverified in browser):**
  - `src/app/layout.tsx`: Roboto → Barlow Condensed (display, var `--font-barlow`) + IBM Plex Sans (body, `--font-plex`) + IBM Plex Mono (data, `--font-plex-mono`).
  - `src/app/globals.css`: zinc-* tokens remapped to warm stone values; radius scale squared (xs 1px … 2xl 4px) + bare `.rounded` → 2px; h1–h3 use display font; `table { font-variant-numeric: tabular-nums }`; `--font-display` theme token.
  - **NOT yet verified — start dev server (`npm run electron:dev` or preview "dev" config) and eyeball every page first.**
- **B. App components — TODO:**
  - `src/components/shared/Modal.tsx`: SubmitButton bg-zinc-900 → engineering blue (blue-700/800 hover); panel look (border, ruled header).
  - `src/components/shared/StatusBadge.tsx`: make pills `rounded-full` explicitly + subtle border (they're the one rounded element).
  - Login + change-password pages: redesign light (paper bg, bordered white panel, ink text, blue primary button, condensed wordmark).
  - Dashboard `src/app/page.tsx`: sidebar stays dark (now warm ink), refine header; consider blue on primary CTAs in tabs (grep `bg-zinc-900` buttons in src/components/tabs/).
  - Field page (`src/app/field/page.tsx`): keep dark; inherits fonts/radius/warm grays automatically.
  - `electron/loading.html`: restyle light + squared + blue bar (self-contained HTML, has its own inline styles/fonts — use fallback stack, no webfont dependency).
- **C. Marketing site restyle — TODO:** rewrite `marketing/index.html` styles to light spec-sheet aesthetic: paper bg, ink text, blue accent, 2px corners, bordered ruled panels instead of shadow cards, replace unicode/emoji icons (⛁✕⇪☎ etc.) with inline stroke SVGs, quiet technical motifs (SEC. 01 labels, hairline crosshair ticks, thin accent rule under headings), restructure hero light/left-aligned. KEEP all working JS demos (jobs ledger click-to-cycle, tabs, phone sync, toasts, counters, form placeholder).
- **D. Logo + rebuild — TODO:** square logo badge corners in `public/logo.svg` (rx 112 → ~56) + the two inline SVG copies (electron/loading.html, marketing/index.html nav); run `node scripts/generate-icons.js`; then `npm run electron:build` (~10 min) so the .exe icon matches. Verify `dist-electron/win-unpacked/resources/nextjs/node_modules/next` exists after any build-config change.

### Design decisions locked in
- Accent: **blue** (engineering blue, ~#1d4ed8/blue-700 family) — user explicitly chose over orange.
- Light-first: dark reserved for sidebar/footer/one marketing band. "It doesn't have to be so dark" — user OK with lighter.
- Typography: Barlow Condensed (headings/wordmark) / IBM Plex Sans (body) / IBM Plex Mono (data, tabular figures).
- Radius: 2px standard, 0 tables, rounded-full only for status pills/dots.
- No glows, no radial gradients, hairline borders for depth.

## Environment facts for the next session
- Dev DB: this machine has a test cluster REMOVED (%APPDATA%\whitevanops wiped). Local Postgres on 5433 NOT running by default. To get a dev DB: `node -e "require('./electron/postgres.js').ensurePostgres({resourcesPath:'<fake resources dir>'})"` pattern used in this session, or start pg_data per office-server memory. Admin password on the last test DB was changed to `WhiteVan2026!` — but that cluster was deleted; fresh bootstraps are admin/admin.
- `.claude/launch.json`: "dev" (npm run dev, port 3000), "marketing" (npx serve marketing -l 4173).
- Lint + `npx tsc --noEmit` were clean before Phase A token changes; re-run after redesign edits.
- Memory files: `project_bundled_postgres.md` has the packaging gotchas.

---

## ⚠️ OPEN ISSUES — address these next time you work on the project

A full-folder security review was done 2026-07-04 (see `CODE_REVIEW_2026-07-04.md` for the ranked
findings, risk/benefit, and fix details). The three critical/high items were fixed **in code** that
session, but each still has **manual follow-up only you can do**, and several lower-severity items
remain untouched. Start here next session.

### 🔴 Firebase key — MUST finish (code done, two manual steps left)
The live Firebase service-account key (`firebase-service-account.json`) was gitignored and
`scripts/license-manager.js` now reads it from `WVO_FIREBASE_SERVICE_ACCOUNT` /
`GOOGLE_APPLICATION_CREDENTIALS`. Still outstanding:
- [ ] **Rotate the key** in the Firebase console (Project Settings → Service accounts → generate new
      key, delete the old one). Treat the copy that was sitting in the repo as compromised.
- [ ] **Move the key file out of the project folder** to e.g. `%APPDATA%\whitevanops-secrets\` and set
      `WVO_FIREBASE_SERVICE_ACCOUNT` to that path. The old file was left in place (now gitignored) so
      you don't lose your only copy before rotating — delete it from the repo once relocated.

### 🟠 Git hygiene before first app commit
The app folder is still NOT a git repo; `.env`, `.env.local`, and the Firebase key live in it.
- [ ] When you version-control the app, init it as a **private** repo (never the public
      `rsm1274-art/WhiteVanOps`, which is the marketing site) with `.gitignore` already in place;
      run `git status` and confirm no `.env*` or `*service-account*.json` is staged before the first commit.
- [ ] Add a secret-scan pre-commit hook (e.g. `gitleaks`) as a backstop.
- [ ] Consider renaming the public marketing repo (e.g. `whitevanops-site`) to kill the name collision.

### 🟠 Licensing (code hardened; optional follow-up)
Local `license.json` is now HMAC-signed and the activation window was moved to the secure Electron
posture. Note for deployment:
- [ ] Existing desktop installs will show the activation screen **once** after the next update (unsigned
      → signed license migration) — keep customers' license keys handy. Documented in Setup manual §12.
- [ ] (Optional) The HMAC secret is baked into the build; for stronger protection consider a
      server-issued signed token + periodic online re-check. Only worth it if piracy resistance matters.

### 🟡 Not yet addressed (from the review — pick up as time allows)
- [ ] **Cookie `Secure` flag divergence** — `login/route.ts` gates `secure` on `REQUIRE_HTTPS` but
      `change-password/route.ts` uses plain `NODE_ENV`; over LAN HTTP a forced-password-change user gets
      silently bounced. Unify via one shared cookie-options helper; document `REQUIRE_HTTPS`.
- [ ] **Backup route command injection** — `src/app/api/settings/backup/route.ts` interpolates an
      admin-set path + `DATABASE_URL` into an `exec` shell string. Switch to `execFile` with an arg array;
      stop returning `error.message` to the client.
- [ ] **No brute-force protection on login** — add per-user/per-IP rate limiting + lockout before any
      external (Tailscale/HTTPS) exposure.

### 🟢 Lower priority (from the review)
- [ ] Tech-without-`personnelId` can change any job's status (`src/app/api/jobs/route.ts:87`) — reject
      instead of skipping the assignment check.
- [ ] No password complexity on admin-created users (`api/users`) — centralize one `validatePassword()`.
- [ ] No automated tests anywhere — add Vitest + cover `jobConflicts`, `recurrence`, `dateUtils`, `auth`.
- [ ] Clutter: `marketing/test.js` (UTF-16 junk) + `extracted.js` scratch files; stale Docker scaffold.
